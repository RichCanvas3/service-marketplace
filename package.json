{
  "private": true,
  "name": "my-mcp-app",
  "scripts": {
    "build": "pnpm -r --filter ./shared... run build && pnpm -r --filter ./client... run build && pnpm -r --filter ./server... run build"
  },
  "devDependencies": {
    "@types/debug": "^4.1.12",
    "typescript": "^5.0.0"
  }
}
